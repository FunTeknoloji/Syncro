package com.example.quakesafe.voice

object OpusCodec {
    // Placeholder for Opus encoding/decoding
    // In a production app, use JNI to call native Opus library

    fun encode(pcm: ByteArray): ByteArray {
        // Return dummy compressed data
        return pcm
    }

    fun decode(opus: ByteArray): ByteArray {
        // Return dummy decompressed data
        return opus
    }
}
